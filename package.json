{
  "name": "dsa-placement-app",
  "version": "1.0.0",
  "private": true,
  "description": "45-Day DSA Placement Preparation - offline React app with localStorage",
  "scripts": {
    "preinstall": "npm install --prefix client",
    "start": "node serve.js",
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  }
}
